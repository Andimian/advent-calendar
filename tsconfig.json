{
  "compilerOptions": {
    "target": "ESNext",                          // Указывает, какой ECMAScript версией будет использоваться для компиляции
    "module": "ESNext",                          // Тип модуля
    "moduleResolution": "node",                  // Метод разрешения модулей (node-style)
    "strict": true,                              // Включает строгий режим
    "jsx": "react-jsx",                          // Режим JSX для React 17+ (если используете React)
    "esModuleInterop": true,                     // Совместимость с CommonJS
    "skipLibCheck": true,                        // Пропуск проверки типов в библиотеках
    "forceConsistentCasingInFileNames": true,    // Требует одинаковый регистр имен файлов
    "outDir": "./dist",                          // Папка для выходных файлов
    "baseUrl": ".",                              // Базовый путь для разрешения модулей
    "paths": {
      "@/*": ["src/*"]                          // Пример псевдонима для путей (можно настроить)
    }
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
